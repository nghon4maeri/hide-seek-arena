# Team Submission Notes — Blind Adversary

Before merging into this folder:
1. Run `python blind/scripts/run_smoke_test.py` to verify
2. Run `python -m pytest blind/tests -v` to pass all tests
3. Run benchmark with blind defaults: `python blind/scripts/benchmark_agents.py --games 10`
