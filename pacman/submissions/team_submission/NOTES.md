# Team Submission Notes

- Final folder controlled by 24127457.
- Keep both `PacmanAgent` and `GhostAgent` available.
- Pacman may return `Move` or `(Move, steps)`.
- Ghost must return `Move` only.
- The Ghost straight-line speed rule is documented in `docs/architecture.md`; no framework movement change is made by this setup.
